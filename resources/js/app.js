import './bootstrap';
// Import the bootstrap-multiselect library
import 'bootstrap-multiselect/dist/js/bootstrap-multiselect';

// Initialize the multiselect on document ready
$(document).ready(function() {
    $('.multiselect').multiselect();
});